// src/analisis/DistribucionGumbelDoble.js
import { row } from "./utils.js";

// TODO: implementar Gumbel doble (mixtura) — requiere EM u optimización
export function compute(xs) {
  return row("—", NaN, "—", NaN);
}
